9.  Задать значения элементам массива с клавиатуры. Определить максимальный элемент и его номер в одномерном массиве. Поиск максимального элемента оформить функцией.  
Поменять местами максимальный элемент с элементом, указанным пользователем.

Выполнено.

```C
#include <stdio.h>

#define MAX_SIZE 100

// Функция для поиска максимального элемента и его индекса
void find_max_element(int arr[], int size, int *max_index, int *max_value) {
    *max_value = arr[0];
    *max_index = 0;
    for (int i = 1; i < size; i++) {
        if (arr[i] > *max_value) {
            *max_value = arr[i];
            *max_index = i;
        }
    }
}  

// Функция для обмена элементов местами

void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}  
int main() {
    int size;
    int arr[MAX_SIZE];
    
    // Ввод размера массива

    printf("Enter array size (max %d): ", MAX_SIZE);
    scanf("%d", &size);
    
    // Ввод элементов массива

    printf("Enter %d array elements:\n", size);
    for (int i = 0; i < size; i++) {
        scanf("%d", &arr[i]);
    }
    int max_index, max_value;
    
    // Поиск максимального элемента

    find_max_element(arr, size, &max_index, &max_value);
    printf("Max element: %d, index: %d\n", max_value, max_index);

    // Ввод индекса элемента для замены

    int user_index;
    printf("Enter the index of the item to exchange with the maximum (0 to %d): ", size - 1);
    scanf("%d", &user_index);
    
    // Проверка на допустимость индекса

    if (user_index >= 0 && user_index < size) {
    
        // Обмен местами

        swap(&arr[max_index], &arr[user_index]);
        printf("Array after exchange:\n");

        // Вывод обновленного массива

        for (int i = 0; i < size; i++) {
            printf("%d ", arr[i]);
        }
        printf("\n");
    } else {
        printf("Error: Invalid index!\n");
    }
    return 0;

}
```

### Объяснение программы:

1. **Объявления и макросы**:
    
    - Мы объявляем максимальный размер массива `MAX_SIZE`, чтобы ограничить размер пользовательского ввода.
2. **Функция `find_max_element`**:
    
    - Эта функция находит максимальный элемент массива и его индекс. Она обновляет переменные, переданные по указателям.
3. **Функция `swap`**:
    
    - Обменяет два элемента местами, принимая указатели на эти элементы.
4. **Основная функция `main`**:
    
    - Запрашивает у пользователя размер массива и сами элементы.
    - Вызывает функцию `find_max_element` для нахождения максимального элемента и выводит его на экран.
    - Запрашивает индекс элемента, с которым пользователь хочет поменять максимальный элемент местами.
    - Проверяет допустимость ввода индекса, и если он валиден, выполняет обмен местами, затем выводит обновленный массив.



### Определение константы


```
#define MAX_SIZE 100
```

- Эта строка определяет макрос `MAX_SIZE`, который ограничивает максимальный размер массива, равный 100.

### Функция поиска максимального элемента


```
void find_max_element(int arr[], int size, int *max_index, int *max_value) {
```

- Эта строка объявляет функцию `find_max_element`, которая принимает:
    - `arr[]`: массив целых чисел,
    - `size`: количество элементов в массиве,
    - `*max_index`: указатель на переменную для хранения индекса максимального элемента,
    - `*max_value`: указатель на переменную для хранения максимального значения.

#### Инициализация


```
    *max_value = arr[0];
    *max_index = 0;
```

- Инициализация `*max_value` значением первого элемента массива, а `*max_index` – индексом 0.

#### Поиск максимального элемента



```
    for (int i = 1; i < size; i++) {
        if (arr[i] > *max_value) {
            *max_value = arr[i];
            *max_index = i;
        }
    }
```

- Цикл начинается с 1 и перебирает все элементы массива. Если текущий элемент `arr[i]` больше текущего `*max_value`, обновляются `*max_value` и `*max_index` соответствующими значениями.

### Функция для обмена элементов


```
void swap(int *a, int *b) {
```

- Эта строка объявляет функцию `swap`, которая принимает два указателя на целые числа `*a` и `*b`.

#### Обмен значениями



```
    int temp = *a;
    *a = *b;
    *b = temp;
```

- Сохраняет значение, на которое указывает `*a`, в временной переменной `temp`. Затем `*a` получает значение `*b`, и `*b` получает значение из `temp`, завершив обмен.

### Главная функция



```
int main() {
```

- Начало главной функции программы, где начинается выполнение.

#### Объявление переменных



```
    int size;
    int arr[MAX_SIZE];
```

- Объявляется переменная `size` для хранения размера массива и статический массив `arr` максимального размера, заданного в `MAX_SIZE`.

#### Ввод размера массива



```
    printf("Enter array size (max %d): ", MAX_SIZE);
    scanf("%d", &size);
```

- Пользователь вводит размер массива, который должен быть не более `MAX_SIZE`.

#### Ввод элементов массива



```
    printf("Enter %d array elements:\n", size);
    for (int i = 0; i < size; i++) {
        scanf("%d", &arr[i]);
    }
```

- Пользователь вводит элементы массива в цикле, который проходит от 0 до `size`.

#### Объявление переменных для хранения максимума



```
    int max_index, max_value;
```

- Объявляются переменные `max_index` и `max_value` для хранения индекса и значения максимального элемента.

#### Поиск максимального элемента


```
    find_max_element(arr, size, &max_index, &max_value);
    printf("Max element: %d, index: %d\n", max_value, max_index);
```

- Вызывается функция `find_max_element` для нахождения максимального элемента в массиве, после чего результат выводится на экран.

### Ввод индекса для замены



```
    int user_index;
    printf("Enter the index of the item to exchange with the maximum (0 to %d): ", size - 1);
    scanf("%d", &user_index);
```

- Объявляется переменная `user_index`, пользователь вводит индекс элемента, который хочет обменять с максимальным.

### Проверка введенного индекса



```
    if (user_index >= 0 && user_index < size) {
```

- Проверяется, находится ли введенный индекс в допустимом диапазоне.

#### Выполнение обмена



```
        swap(&arr[max_index], &arr[user_index]);
        printf("Array after exchange:\n");
```

- Если индекс допустимый, вызывается функция `swap`, которая меняет местами максимальный элемент и элемент по индексу `user_index`. Затем начинается вывод обновленного массива.

#### Вывод обновленного массива



```
        for (int i = 0; i < size; i++) {
            printf("%d ", arr[i]);
        }
        printf("\n");
```

- Цикл выводит все элементы массива после обмена, и наконец добавляется перевод строки.

#### Ошибка при некорректном индексе



```
    } else {
        printf("Error: Invalid index!\n");
    }
```

- Если индекс невалидный, выводится сообщение об ошибке.

### Завершение программы


```
    return 0;
}
```

- Возврат 0 указывает на успешное завершение программы.